'use strict';

let money, 
    start = function(){
        do {
            money = prompt( 'Ваш месячный доход?' );
       }
       while (isNaN(money) || money == '' || money ==null)
    };

  start();


let appData = {
    income: {},
    addIncome: [],
    expenses: {},
    expensesRepeat: {},
    addExpenses: [],
    deposit: false,
    mission: 50000,
    budget: money,
    budgetDay: 0, 
    budgetMonth: 0,  
    expensesMonth: 0,
    period: 12,
    asking: function(){
        let addExpenses = prompt( 'Перечислите возможные расходы за ' +  
                'рассчитываемый период через запятую'.split(',') );
            appData.addExpenses = addExpenses.toLowerCase().split(',');
            appData.deposit = confirm( 'Есть ли у Вас депозит в банке?' );
    },
       
    // getExpensesMonth: function () {
    // let sum = 0,
    //     expensesAmount = expensesMonth();
    // for (let i=0; i <2; i++){
    //     if (i === 0){
    //         expenses = prompt( 'Какие обязательные ежемесячные расходы у вас есть?', 'Кредиты' );
    //       } else if(i === 1){
    //         expensesRepeat = prompt( 'Какие еще обязательные ежемесячные расходы у вас есть?', 'Квартплата' );
    //       }
    //       let counter;
    //       do {
    //         counter = prompt( 'Во сколько это обойдется?' );
    //       } while (isNaN(counter) || counter == '' || counter ==null)
    //         sum += +counter;
                           
    //     }

    // return sum;
    // console.log('Расходы за месяц: ', expensesAmount);
    // },

    accumulatedMonth: function() {
        accumulatedMonth  = ( money - appData.expensesMonth ); //аналогичные действия
        
        console.log( 'Накопления за месяц: ', appData.accumulatedMonth ); 
    },

    // getAccumulatedMonth: function() {
    //     let accumulatedMonth = getAccumulatedMonth();
    //         return ( money - expensesAmount ); //аналогичные действия
    //     console.log( 'Накопления за месяц: ', accumulatedMonth ); 
    // },

    // getTargetMonth: function() {
    //     let targetMonth = +Math.floor(appData.mission / accumulatedMonth);
    //    if(targetMonth < 0){
    //         return('Цель не будет достигнута');    
    //     } else{
    //         return('Цель будет достигнута через: ' + targetMonth + ' месяца(ев)');
    //     }
    //     console.log(getTargetMonth());
    // },

    getStatusIncome: function(){
        let appData.budgetDay = Math.floor( appData.accumulatedMonth /30 );
        if (appData.budgetDay >= 0 && budgetDay < 300){
            return('Низкий');
        } else if (budgetDay >= 300 && budgetDay < 800){
            return('Средний');
        } else if (budgetDay >= 800){
            return('Высокий');
        } else {
            return('Что-то пошло не так'); 
        }
    }

};

